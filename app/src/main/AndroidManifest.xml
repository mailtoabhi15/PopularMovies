<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udacitynanodegreeapps.android.popmovretro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_web"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.udacitynanodegreeapps.android.popmovretro.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop" >
            <!--Dixit:
              1) If ParentActivity has launchMode as singleTop then parentactivity is brough to the top of the stack
               and receives the intent through its onNewIntent() Method.
              2) If ParentActivity has launchMode as standard the parent activity is popped of the stack and a new instance
              of that activity is created on top of the stack to receive the intent-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.udacitynanodegreeapps.android.popmovretro.DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName="com.udacitynanodegreeapps.android.popmovretro.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.udacitynanodegreeapps.android.popmovretro.MainActivity" />
        </activity>
        <activity
            android:name="com.udacitynanodegreeapps.android.popmovretro.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.udacitynanodegreeapps.android.popmovretro.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.udacitynanodegreeapps.android.popmovretro.MainActivity" />
        </activity>
    </application>

</manifest>
